# Tasks Pendentes

## Produção

- [ ] Configurar `RESEND_API_KEY` e `RESEND_FROM_EMAIL` com domínio próprio verificado
      no Resend — hoje está usando `onboarding@resend.dev`, que só envia para o
      e-mail da conta Resend e não funciona para usuários reais.
      Referência: resend.com → Domains → Add Domain

## Formato das tasks

Usar [ ] para pendente e [x] para concluído.
Adicionar novas tasks neste arquivo sempre que surgirem pendências relevantes.
